@model Facturacion.Models.FacturaCabecera

@{
    ViewData["Title"] = "Details";
}

<h1 class="mb-4">Detalles de Factura</h1>

<div class="border p-3">
    <h4>FacturaCabecera</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FechaFactura)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FechaFactura)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Total)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Total)
        </dd>
        <dt class="col-sm-2">
            Cliente
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdClienteNavigation.Nombre)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.IdFacturaCabecera"><i class="bi bi-pencil"></i> Editar</a> |
    <a class="btn btn-secondary" asp-action="Index"><i class="bi bi-arrow-left"></i> Volver al Listado</a>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model?.IdFacturaCabecera">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
