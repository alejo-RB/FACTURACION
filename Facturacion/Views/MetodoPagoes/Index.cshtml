@model IEnumerable<Facturacion.Models.MetodoPago>

@{
    ViewData["Title"] = "Index";
}

<h1 class="mb-4">Listado de Métodos de Pago</h1>

<p>
    <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus"></i> Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@item.IdMetodoPago"><i class="bi bi-pencil"></i> Editar</a> |
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.IdMetodoPago"><i class="bi bi-info-circle"></i> Detalles</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.IdMetodoPago"><i class="bi bi-trash"></i> Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
