@model Facturacion.Models.MetodoPago

@{
    ViewData["Title"] = "Details";
}

<h1 class="mb-4">Detalles del Método de Pago</h1>

<div class="border p-3">
    <h4 class="mb-3">Método de Pago</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@Model?.IdMetodoPago"><i class="bi bi-pencil"></i> Editar</a> |
    <a class="btn btn-info" asp-action="Index"><i class="bi bi-arrow-left"></i> Volver al Listado</a>
</div>

